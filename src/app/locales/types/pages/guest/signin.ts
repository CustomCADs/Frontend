export type Login = {
	title: string;
	subtitle: string;
	login: string;
	'google-login': string;
	'register-message': string;
	register: string;
};

export type Forgot = {
	title: string;
	subtitle: string;
	button: string;
	message: string;
};

export type Reset = {
	title: string;
	subtitle: string;
	button: string;
	message: string;
};
