export type Errors = {
	required: string;
	length: string;
	pattern: string;
};

export type Labels = {
	username: string;
	email: string;
	password: string;
	'remember-me': string;
	'forgot-password': string;
};

export type Placeholders = {
	'search-products': string;
	username: string;
	email: string;
	password: string;
};
