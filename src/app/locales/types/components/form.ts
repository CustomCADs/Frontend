export type Errors = {
	required: string;
	length: string;
};

export type Labels = {
	username: string;
	password: string;
	'remember-me': string;
	'forgot-password': string;
};

export type Placeholders = {
	username: string;
	password: string;
};
